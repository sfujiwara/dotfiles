version: 2
jobs:

  test1:
    working_directory: ~/dotfiles
    # docker:
    #   - image: circleci/python:3.7
    macos:
      xcode: "10.0.0"
    steps:
      - checkout
      - run: echo hello

  symbolic_links:
    working_directory: ~/dotfiles
    docker:
      - image: circleci/python:3.7
    steps:
      - checkout
      - run: make symbolic-links

workflows:
  version: 2
  build_and_test:
    jobs:
      - test1
      - symbolic_links